name: "2 - [DEV] Build & Deploy"

on:
  pull_request:
    types: closed
    branches: develop

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build
        run: echo Building the app
